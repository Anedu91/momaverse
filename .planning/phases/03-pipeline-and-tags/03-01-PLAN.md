---
phase: 03-pipeline-and-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/extractor.py
  - pipeline/processor.py
  - database/seeds/websites_ba.sql
  - database/seeds/tag_rules_ba.sql
autonomous: false

must_haves:
  truths:
    - "Pipeline extracts events from Buenos Aires sources, not NYC"
    - "Events extracted from Spanish-language pages get English tags"
    - "BA-redundant tags (buenosaires, caba, argentina) are excluded from output"
    - "Pipeline has configured Argentine event source websites to crawl"
  artifacts:
    - path: "pipeline/extractor.py"
      provides: "BA-adapted Gemini prompts"
      contains: "Buenos Aires"
    - path: "pipeline/processor.py"
      provides: "BA location normalization (no NYC boroughs)"
    - path: "database/seeds/websites_ba.sql"
      provides: "Website + URL + tag + location seed data"
      contains: "INSERT INTO websites"
    - path: "database/seeds/tag_rules_ba.sql"
      provides: "Spanish-to-English rewrites and BA exclusion rules"
      contains: "INSERT INTO tag_rules"
  key_links:
    - from: "pipeline/extractor.py"
      to: "Gemini API"
      via: "prompt text"
      pattern: "Buenos Aires"
    - from: "database/seeds/websites_ba.sql"
      to: "database/seeds/*_ba.sql (Phase 2 locations)"
      via: "website_locations FK to locations"
      pattern: "INSERT INTO website_locations"
---

<objective>
Adapt the pipeline for Buenos Aires: replace NYC references in Gemini prompts with BA context, clean up NYC-specific location normalization in the processor, and create SQL seed files for Argentine event source websites and tag rules.

Purpose: The pipeline currently has hardcoded NYC strings in extraction prompts and NYC borough names in location matching. This plan makes the pipeline BA-aware so it can crawl Argentine sources and extract Spanish-language events correctly.

Output: Modified extractor.py and processor.py, plus two new SQL seed files (websites_ba.sql, tag_rules_ba.sql) ready to be run against the database.

NOTE on tags.json: The checker flagged that tags.json is not updated with BA cultural tags. This is a non-issue because:
- tags.json only contains two things: `geotags` (barrio names for geo-filtering) and `bgcolors` (emoji-to-color map)
- Geotags were already updated to BA barrios in Phase 1
- Category/cultural tags (Art, Theater, Music, etc.) are NOT registered in tags.json
- The tag cloud is built DYNAMICALLY from event data via `buildTagIndex()` in `src/js/data/dataManager.js` (lines ~419-432: `event.tags.forEach(tag => allUniqueTagsSet.add(tag))`)
- Therefore: pipeline produces events with tags -> tags appear in tag cloud automatically. No static tag registration needed.
</objective>

<execution_context>
@/Users/anedu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anedu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline-and-tags/03-RESEARCH.md
@pipeline/extractor.py
@pipeline/processor.py
@database/schema.sql
@database/seeds/museos_ba.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt pipeline Python files for Buenos Aires</name>
  <files>pipeline/extractor.py, pipeline/processor.py</files>
  <action>
In extractor.py, replace all 4 NYC references with Buenos Aires equivalents:

1. Line ~604 (`extract_chunk` function): Change `"Extract ALL events from this NYC events page"` to `"Extract ALL events from this Buenos Aires events page"`

2. Line ~734 (`get_prompt` function): Change `"upcoming events in New York City"` to `"upcoming events in Buenos Aires, Argentina"`

3. Line ~747: Change `"Avoid location-specific or NYC-redundant tags"` to `"Avoid location-specific or Buenos Aires-redundant tags. Generate all tags in ENGLISH even though source content may be in Spanish."`

4. Line ~753: Change `"Only include events in the NYC area"` to `"Only include events in the Buenos Aires area"`. Also add a note about Argentine date formats: `"Dates on Argentine sites often use DD/MM/YYYY format and Spanish month names."`

In processor.py, clean up NYC-specific code:

1. Lines ~219-221: Remove the NYC prefix/suffix stripping regex lines (the two `re.sub` lines that strip "NYC" from tags). These are harmless no-ops for BA but should be removed for cleanliness.

2. Lines ~422-450 (`_normalize_location_name` function): Replace the NYC borough list (`['queens', 'bronx', 'brooklyn', 'manhattan', 'staten island']`) and the suffixes list (`['nyc', 'new york', 'brooklyn', 'manhattan', 'queens', 'bronx', 'staten island']`) with an empty list. Keep the function structure intact (the virtual/online check, the "the " prefix strip, the suffix stripping logic) -- just remove the NYC-specific data. The BA venues use alternate_names for matching so borough-style normalization is not needed.

Do NOT change any other files. Do NOT modify crawler.py, merger.py, exporter.py, main.py, or db.py.
  </action>
  <verify>
Run: `grep -n "NYC\|New York\|nyc\|new york\|queens\|bronx\|brooklyn\|manhattan\|staten island" pipeline/extractor.py pipeline/processor.py` -- should return zero matches.
Run: `grep -n "Buenos Aires" pipeline/extractor.py` -- should return matches at the 4 prompt locations.
Run: `python3 -c "import pipeline.extractor; import pipeline.processor; print('imports OK')"` from the project root to verify no syntax errors (may fail if dependencies not installed locally -- that's OK, syntax check is the goal).
  </verify>
  <done>All NYC/New York references removed from extractor.py and processor.py. Prompts reference Buenos Aires. Tags instruction explicitly says English. NYC borough list emptied from location normalization.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which Argentine event source websites should the pipeline crawl?</decision>
  <context>
The pipeline needs website entries in the database to know what to crawl. PROJECT.md says "User has Buenos Aires event sources identified and ready to configure." Each website needs:
- Name and base URL
- One or more URLs to crawl (agenda/events pages)
- Optional: link to a Phase 2 location (if the source is a venue we already have)
- Optional: extra tags to auto-apply to all events from this source
- Optional: notes to guide Gemini extraction (e.g., "Events are in Spanish, page uses infinite scroll")
- Optional: crawl settings (selector for click-to-load, js_code, keywords, etc.)

Examples of the kind of sources that work well:
- Venue agenda pages (e.g., cck.gob.ar/agenda, malba.org.ar/agenda)
- Event aggregator sites (e.g., alternativa teatral, Buenos Aires cultural agenda)
- Gallery/museum exhibition pages

Please provide your list of Argentine event sources. For each, give at minimum:
1. Website name
2. URL(s) to crawl
3. Any special notes (needs JavaScript rendering, has pagination, etc.)
  </context>
  <options>
    <option id="provide-sources">
      <name>Provide source list now</name>
      <pros>Plan can create complete seed SQL immediately</pros>
      <cons>None</cons>
    </option>
    <option id="placeholder-sources">
      <name>Use placeholder sources for now, add real ones later</name>
      <pros>Unblocks plan completion</pros>
      <cons>Pipeline won't produce real events until real sources are added</cons>
    </option>
  </options>
  <resume-signal>Provide your list of sources, or say "placeholder" to use example sources that can be updated later</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create SQL seed files for websites and tag rules</name>
  <files>database/seeds/websites_ba.sql, database/seeds/tag_rules_ba.sql</files>
  <action>
Create two SQL seed files following the same pattern as existing seeds (e.g., museos_ba.sql: USE fomo; comment header; INSERT statements).

**database/seeds/tag_rules_ba.sql:**
Insert tag rules for Buenos Aires pipeline:

Rewrite rules (Spanish -> English):
- musica -> Music, danza -> Dance, teatro -> Theater, arte -> Art
- exposicion -> Exhibition, concierto -> Concert, espectaculo -> Show
- gratis -> Free Entry, entradalibre -> Free Entry, entradalibr -> Free Entry
- fotografia -> Photography, literatura -> Literature, pelicula -> Film
- taller -> Workshop, charla -> Talk, conferencia -> Lecture
- feria -> Fair, mercado -> Market, festival -> Festival
- circo -> Circus, comedia -> Comedy, poesia -> Poetry

Exclude rules (BA-redundant tags):
- buenosaires, argentina, caba, ba, ciudaddebuenosaires

**database/seeds/websites_ba.sql:**
Use the sources provided by the user in the checkpoint decision (Task 2). For each website:

1. INSERT INTO websites with: name, description, base_url, crawl_frequency (default 3 days), notes (include "Events are in Spanish. Dates may use DD/MM/YYYY format." plus any user-provided notes), source_type ('primary' for venues, 'aggregator' for listing sites).

2. INSERT INTO website_urls with the crawl URLs.

3. INSERT INTO website_locations linking to Phase 2 locations where applicable (use subquery: `SELECT id FROM locations WHERE name = '...' LIMIT 1`).

4. INSERT INTO website_tags with relevant category tags for each source.

Follow the existing seed pattern: SET @var_id with subquery, then use @var_id in subsequent inserts. Include clear comment headers separating each website block.
  </action>
  <verify>
Run: `mysql --help > /dev/null 2>&1 && echo "mysql available" || echo "mysql not available"` -- if mysql CLI is available, syntax-check the SQL files: `mysql --user=root fomo < database/seeds/tag_rules_ba.sql --verbose 2>&1 | head -20`. Otherwise, visually verify SQL is syntactically correct (matching parens, correct column counts, proper quoting).
Verify: tag_rules_ba.sql has at least 15 rewrite rules and 5 exclude rules.
Verify: websites_ba.sql has INSERT statements for websites, website_urls, and website_tags tables.
  </verify>
  <done>SQL seed files exist with complete tag rules for Spanish->English mapping and BA exclusions, plus website configuration for all user-provided Argentine event sources with URLs, location links, and extra tags.</done>
</task>

</tasks>

<verification>
1. `grep -rn "NYC\|New York\|nyc\|new york" pipeline/extractor.py pipeline/processor.py` returns no matches
2. `grep -c "Buenos Aires" pipeline/extractor.py` returns 4 (one per prompt location)
3. `grep -c "INSERT INTO tag_rules" database/seeds/tag_rules_ba.sql` returns at least 20
4. `grep -c "INSERT INTO websites" database/seeds/websites_ba.sql` returns at least 1
5. No changes to crawler.py, merger.py, exporter.py, main.py, db.py
6. tags.json is NOT modified (category tags flow dynamically from event data via buildTagIndex, no static registration needed)
</verification>

<success_criteria>
- Pipeline prompts say "Buenos Aires" everywhere NYC was mentioned
- Tags prompt explicitly requires English output from Spanish content
- NYC borough normalization removed from processor.py
- tag_rules_ba.sql has Spanish->English rewrites and BA exclusions
- websites_ba.sql configures user's Argentine event sources with proper FK links
- All SQL follows existing seed file conventions (USE fomo, comment headers, SET @var pattern)
- tags.json unchanged -- confirmed that category tags are dynamic (built from event data in buildTagIndex)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-and-tags/03-01-SUMMARY.md`
</output>

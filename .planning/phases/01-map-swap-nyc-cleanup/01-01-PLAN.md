---
phase: 01-map-swap-nyc-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js/script.js
  - pipeline/exporter.py
autonomous: true

must_haves:
  truths:
    - "Opening the app shows a map centered on Buenos Aires (approx -34.61, -58.40) at zoom 13"
    - "Panning and zooming are constrained to the Buenos Aires metro area via maxBounds"
    - "User geolocation validation checks Buenos Aires bounds, not NYC"
    - "Pipeline exporter INIT_LAT_RANGE and INIT_LNG_RANGE target Buenos Aires core area"
  artifacts:
    - path: "src/js/script.js"
      provides: "BA map config, city bounds, maxBounds on map, renamed geolocation function"
      contains: "MAP_INITIAL_VIEW.*-34"
    - path: "pipeline/exporter.py"
      provides: "BA bounding box for init dataset splitting"
      contains: "INIT_LAT_RANGE.*-34"
  key_links:
    - from: "src/js/script.js App.config.CITY_BOUNDS"
      to: "isWithinCityBounds() function"
      via: "config property reference"
      pattern: "this\\.config\\.CITY_BOUNDS"
    - from: "src/js/script.js App.config.CITY_BOUNDS"
      to: "maplibregl.Map maxBounds option"
      via: "map constructor options"
      pattern: "maxBounds.*CITY_BOUNDS"
---

<objective>
Repoint the map from New York City to Buenos Aires by updating all coordinate configuration, adding MapLibre maxBounds to constrain panning, renaming the geolocation validation function, and updating the pipeline exporter bounding box.

Purpose: This is the functional core of the city swap -- after this plan, the map renders Buenos Aires at the correct location with proper bounds constraining.
Output: Updated script.js with BA coordinates and bounds, updated exporter.py with BA init area coordinates.
</objective>

<execution_context>
@/Users/anedu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anedu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-swap-nyc-cleanup/01-RESEARCH.md
@src/js/script.js
@pipeline/exporter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.config and geolocation in script.js</name>
  <files>src/js/script.js</files>
  <action>
  In `src/js/script.js`, make the following changes:

  1. **App.config changes (around lines 124-137):**
     - Change `MAP_INITIAL_VIEW` from `[40.70424, -73.97086]` to `[-34.6083, -58.4000]` (Buenos Aires center, slightly west to center on dense cultural barrios)
     - Change `MAP_INITIAL_ZOOM` from `12` to `13` (BA urban core is more compact than NYC)
     - Rename `NYC_BOUNDS` to `CITY_BOUNDS` and update values:
       ```javascript
       CITY_BOUNDS: {
           latMin: -34.75,
           latMax: -34.50,
           lngMin: -58.60,
           lngMax: -58.28
       },
       ```
     - Update the comment above bounds from "NYC area bounds" to "Buenos Aires metro area bounds for geolocation validation"
     - Bump the style URL version query params from `?v=7` to `?v=8` on both MAP_STYLE_DARK and MAP_STYLE_LIGHT (cache bust after style name changes in plan 02)

  2. **isWithinNYC -> isWithinCityBounds (around line 589):**
     - Rename the function from `isWithinNYC` to `isWithinCityBounds`
     - Update it to reference `this.config.CITY_BOUNDS` instead of `this.config.NYC_BOUNDS`
     - Update the JSDoc comment to say "Check if coordinates are within city bounds" instead of "within NYC bounds"

  3. **getUserLocation (around line 601):**
     - Update the JSDoc to say "outside city bounds" instead of "outside NYC"
     - Update the inline comment from "Only use location if within NYC area" to "Only use location if within city bounds"
     - Change `this.isWithinNYC(lat, lng)` to `this.isWithinCityBounds(lat, lng)`

  4. **initMap maxBounds (around line 687):**
     - Add `maxBounds` option to the `new maplibregl.Map()` constructor, after the `maxZoom` line:
       ```javascript
       maxBounds: [
           [this.config.CITY_BOUNDS.lngMin, this.config.CITY_BOUNDS.latMin],
           [this.config.CITY_BOUNDS.lngMax, this.config.CITY_BOUNDS.latMax]
       ],
       ```
     - MapLibre `maxBounds` accepts `LngLatBoundsLike` format: `[[swLng, swLat], [neLng, neLat]]`

  5. **Update all remaining NYC comments in script.js:**
     - Search for any remaining comments mentioning "NYC" or "New York" and update them to reference "Buenos Aires" or use city-agnostic language. The research identified comments at approximately lines 127, 597, 618, 650, and 674.

  Do NOT change any other behavior -- this is purely config values, a function rename, and a new map constructor option.
  </action>
  <verify>
  Run these checks:
  - `grep -n "NYC" src/js/script.js` should return zero results
  - `grep -n "CITY_BOUNDS" src/js/script.js` should show the config definition and usage in isWithinCityBounds and initMap
  - `grep -n "MAP_INITIAL_VIEW" src/js/script.js` should show the BA coordinates (-34.6083, -58.4000)
  - `grep -n "maxBounds" src/js/script.js` should show the maxBounds option in initMap
  - `grep -n "isWithinNYC" src/js/script.js` should return zero results
  </verify>
  <done>
  - MAP_INITIAL_VIEW is [-34.6083, -58.4000], MAP_INITIAL_ZOOM is 13
  - NYC_BOUNDS renamed to CITY_BOUNDS with BA coordinates (latMin: -34.75, latMax: -34.50, lngMin: -58.60, lngMax: -58.28)
  - isWithinNYC renamed to isWithinCityBounds, references updated
  - maxBounds added to Map constructor using CITY_BOUNDS values
  - Zero occurrences of "NYC" or "isWithinNYC" remain in script.js
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pipeline exporter bounding box</name>
  <files>pipeline/exporter.py</files>
  <action>
  In `pipeline/exporter.py`, make the following changes:

  1. **Update INIT_LAT_RANGE and INIT_LNG_RANGE (lines 13-14):**
     - Change `INIT_LAT_RANGE = (40.672945, 40.735535)` to `INIT_LAT_RANGE = (-34.63, -34.57)`
     - Change `INIT_LNG_RANGE = (-73.998595, -73.943125)` to `INIT_LNG_RANGE = (-58.44, -58.36)`
     - These cover the central Buenos Aires area (Palermo, Recoleta, San Telmo, La Boca, Microcentro) for the initial dataset split

  2. **Update the comment above (line 12):**
     - Change from `# Bounding box for the "init" set (NYC core area)` to `# Bounding box for the "init" set (Buenos Aires core area)`

  3. **Search for any other NYC/New York references in exporter.py and update them.**
     - The research identified comments at approximately lines 12 and 36. The docstring at line 36 mentions "core NYC area" -- update to "core Buenos Aires area".

  Do NOT change any logic -- only coordinate values and comments.
  </action>
  <verify>
  Run these checks:
  - `grep -n "NYC\|New York\|nyc" pipeline/exporter.py` should return zero results
  - `grep -n "INIT_LAT_RANGE" pipeline/exporter.py` should show the BA coordinates (-34.63, -34.57)
  - `grep -n "INIT_LNG_RANGE" pipeline/exporter.py` should show the BA coordinates (-58.44, -58.36)
  </verify>
  <done>
  - INIT_LAT_RANGE is (-34.63, -34.57), INIT_LNG_RANGE is (-58.44, -58.36)
  - All NYC references removed from exporter.py
  - Comment updated to reference Buenos Aires
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "NYC\|isWithinNYC" src/js/script.js pipeline/exporter.py` returns zero results
2. `grep -n "CITY_BOUNDS" src/js/script.js` shows config definition and two usages (isWithinCityBounds, maxBounds)
3. `grep -n "Buenos Aires" src/js/script.js pipeline/exporter.py` shows updated comments
4. Build succeeds: `npm run build` completes without errors (if build script exists and is runnable)
</verification>

<success_criteria>
- The map configuration points to Buenos Aires (-34.6083, -58.4000) at zoom 13
- maxBounds constrains the map to Buenos Aires metro area
- Geolocation validation checks Buenos Aires bounds using city-agnostic function name
- Pipeline exporter targets Buenos Aires core area for init dataset
- Zero NYC references remain in script.js or exporter.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-swap-nyc-cleanup/01-01-SUMMARY.md`
</output>

{
  "private": true,
  "scripts": {
    "dev": "node build.js --dev --watch",
    "build": "node build.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "flatpickr": "^4.6.13"
  }
}
